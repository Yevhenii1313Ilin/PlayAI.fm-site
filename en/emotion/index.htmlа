<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>PlayAI Emotion Hub</title>
  <style>
    body {
      margin: 0;
      background: #0b0b0b;
      color: #fff;
      font-family: 'Segoe UI', sans-serif;
      text-align: center;
      padding: 4rem 2rem;
      height: 100vh;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }

    h1 {
      font-size: 2.6rem;
      background: linear-gradient(90deg, #6cf, #f6f);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 3rem;
      animation: pulse 4s infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.7; }
    }

    .buttons {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 2rem;
      max-width: 800px;
    }

    .button {
      background: #1a1a1a;
      padding: 1.2rem 2rem;
      border-radius: 14px;
      box-shadow: 0 0 20px rgba(102, 204, 255, 0.2);
      text-decoration: none;
      color: #6cf;
      font-weight: bold;
      font-size: 1.2rem;
      transition: 0.3s ease;
      animation: breathe 5s infinite ease-in-out;
    }

    .button:hover {
      background: #222;
      box-shadow: 0 0 32px rgba(255, 102, 255, 0.3);
      transform: scale(1.05);
    }

    @keyframes breathe {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.05); }
    }

    footer {
      margin-top: 4rem;
      font-size: 0.9rem;
      color: #444;
    }
  </style>
</head>
<body>

  <h1>PlayAI Emotion Hub</h1>

  <div class="buttons">
    <a class="button" href="/en/pulse">Pulse</a>
    <a class="button" href="/en/flow">Flow</a>
    <a class="button" href="/en/echo">Echo</a>
    <a class="button" href="/en/journal">Journal</a>
    <a class="button" href="/en/signal">Signal</a>
  </div>

  <footer>
    PlayAI Emotion Hub · 2025
  </footer>

</body>
</html>
